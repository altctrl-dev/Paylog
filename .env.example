# Database
# Local development (PostgreSQL 17):
DATABASE_URL="postgresql://YOUR_USERNAME@localhost:5432/paylog_dev"
# Production (Railway):
# DATABASE_URL="postgresql://postgres:PASSWORD@HOST:PORT/railway"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here" # Generate with: openssl rand -base64 32

# Microsoft Entra ID (Azure AD) - Used for BOTH OAuth login AND SharePoint storage
# Create app registration at: https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps
# Required redirect URIs for OAuth:
#   - http://localhost:3000/api/auth/callback/microsoft-entra-id (development)
#   - https://your-domain.com/api/auth/callback/microsoft-entra-id (production)
# Required API permissions:
#   - User.Read (Delegated) - for OAuth login
#   - Files.ReadWrite.All (Application) - for SharePoint storage
#   - Sites.ReadWrite.All (Application) - for SharePoint storage
# Note: AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID are used by both
# OAuth authentication and SharePoint storage (configured below in Storage section)

# App
NODE_ENV="development"

# Email Configuration
EMAIL_ENABLED=true
RESEND_API_KEY=re_cJ73o2EC_N2Xc1p3xLznWCjwFm2JXgdYP
EMAIL_FROM=notifications@servsys.com
ADMIN_EMAILS=althaf@servsys.com,admin2@servsys.com
EMAIL_PREVIEW=true

# Storage Configuration
# Providers: local, sharepoint, s3, r2
STORAGE_PROVIDER=local
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
MAX_FILES_PER_INVOICE=10
ALLOWED_FILE_TYPES=pdf,png,jpg,jpeg,docx,xlsx,csv,txt

# SharePoint/OneDrive Storage (when STORAGE_PROVIDER=sharepoint)
# Requires Azure AD App Registration with Microsoft Graph API permissions:
# - Files.ReadWrite.All (Application permission)
# - Sites.ReadWrite.All (Application permission)
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-app-client-id
# AZURE_CLIENT_SECRET=your-app-client-secret
# SHAREPOINT_SITE_ID=your-sharepoint-site-id
# SHAREPOINT_DRIVE_ID=optional-specific-drive-id
# SHAREPOINT_BASE_FOLDER=Paylog
